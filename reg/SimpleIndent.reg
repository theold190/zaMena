REGEDIT4

[HKEY_CURRENT_USER\Software\far2\KeyMacros\Editor\i]
"Sequence"="$If(!Selected)   $AKey $Else   editor.undo(0);   Eval(%%GetInitialPosition)    editor.sel(4);    %tabSize=-1;   Eval(%%GetTabSize)    $If(%endColumn==0 && %startLine!=%endLine)     %endLine=%endLine-1;   $End    %iterStart=%startLine;   %iterEnd=%endLine;   $While(%iterStart < %iterEnd)      editor.pos(1,1,%iterStart);     $While(index(Editor.Value, \"{\") == -1 && %iterStart <= %iterEnd)       %iterStart=%iterStart+1;       editor.pos(1,1,%iterStart);     $End      editor.pos(1,1,%iterEnd);     $While(index(Editor.Value, \"}\") == -1 && %iterStart <= %iterEnd)       %iterEnd=%iterEnd-1;       editor.pos(1,1,%iterEnd);     $End      $If(%iterStart < %iterEnd)       %startFormatLine=%iterStart;       %endFormatLine=%iterEnd;       Eval(%%FormatCodeBlock)     $End      %iterStart=%iterStart+1;     %iterEnd=%iterEnd-1;   $End    editor.pos(1,2,%positionColumn);   editor.pos(1,1,%positionLine);    editor.undo(1); $End"
"DisableOutput"=dword:00000001
